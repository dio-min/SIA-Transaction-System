export const Addfee = (studID) => {

    // find stud id in database
    // if found, add fee to the student record
    // if not found, return error message
    // return success message if fee is added successfully

  return (
    <div>
        <h1>e</h1>
    </div>
    )
}

